// SPDX-License-Identifier: GPL-3.0-or-later
pragma solidity >=0.8.12 <0.9.0;



contract EthosIdo123123{

   

    function setStartTimeAndPrice(uint startTime, uint min) external {
       
    }

    function count() external view returns(uint){
    }

    
    function _getThawProportion(uint256 date) internal view returns(uint256 proportion){
       
    }

    function invest(uint amount, string memory ref, string memory slug) external {
    }

    function expected(address account, uint time) public view returns(uint total, uint balance, uint additional){
    }

    function getRewards() public {
    }

    function withdraw() external {
    }


     struct ImportApiLog{
        string provicer;
        uint amount;
        string ref;
        uint time;
        address account;
    }


    function importInvestApiLog(ImportApiLog[] memory importLog) external {
    }


     struct ImportLog{
        uint amount;
        uint ref;
        uint payed;
        address account;
    }

    function importInvestLog(ImportLog[] memory importLog) external {
        
    }


    function exportInvestLog(uint start, uint end) external view returns(ImportLog[] memory log){
       
    }


}


